#include<iostream>
using namespace std;
int main()
{

    long long  a, b, c, d;
    long long sum;
    cin >> a >> b >> c >> d;
    sum = (a * b) - (c * d);
    cout << "Difference = " << sum;

    return 0;
}